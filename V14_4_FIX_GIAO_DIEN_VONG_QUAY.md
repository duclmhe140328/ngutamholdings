# V14.4 — Sửa giao diện vòng quay

- Thay nhãn HTML tuyệt đối bằng SVG 8 lát cố định.
- Mỗi giá trị nằm giữa đúng lát của nó, không chồng nhau.
- Không còn các pill trắng che lát bánh xe.
- Kim vẫn khớp `rewardIndex` do backend trả về.
- Hỗ trợ mobile, tablet và PWA.
- Cache PWA mới: `foodhub-pwa-v14-4-svg-wheel`.
